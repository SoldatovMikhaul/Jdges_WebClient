package com.practice.mainApplication.repository;

import com.practice.mainApplication.models.RegionRF;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegionRFRepository extends JpaRepository<RegionRF, Long> {
}
