package com.practice.mainApplication.repository;

import com.practice.mainApplication.models.TitleRF;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TitleRFRepository extends JpaRepository<TitleRF, Long> {
}
