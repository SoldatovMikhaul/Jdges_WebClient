package com.practice.mainApplication.repository;

import com.practice.mainApplication.models.CategoryRF;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CategoryRFRepository extends JpaRepository<CategoryRF, Long> {
}
