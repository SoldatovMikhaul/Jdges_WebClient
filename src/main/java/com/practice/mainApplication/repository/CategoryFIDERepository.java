package com.practice.mainApplication.repository;

import com.practice.mainApplication.models.CategoryFIDE;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CategoryFIDERepository extends JpaRepository<CategoryFIDE, Long> {
}
